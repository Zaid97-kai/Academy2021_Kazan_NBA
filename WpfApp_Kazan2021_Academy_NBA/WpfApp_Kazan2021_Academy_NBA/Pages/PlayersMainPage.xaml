<Page x:Class="WpfApp_Kazan2021_Academy_NBA.Pages.PlayersMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_Kazan2021_Academy_NBA.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Players Main">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="{StaticResource MainBlue}">
            <Label>Season:</Label>
            <ComboBox Name="CmbSeasons"></ComboBox>
            <Label>Team:</Label>
            <ComboBox Name="CmbTeams"></ComboBox>
            <Label>Player name:</Label>
            <TextBox Name="TxtPlayerName"></TextBox>
        </StackPanel>
        <DataGrid Name="GridPlayers" Grid.Row="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding }"></Image>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="No." Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Team" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Position" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Weight" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Height" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Experience" Binding="{Binding }"></DataGridTextColumn>
                <DataGridTextColumn Header="Country" Binding="{Binding }"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="2"></Grid>
    </Grid>
</Page>
